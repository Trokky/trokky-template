{
  "name": "trokky-template",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "A magazine site on Trokky, ready to deploy to Cloudflare Workers in one click: Astro frontend, Trokky API, Studio.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/copy-studio.mjs && astro dev",
    "build": "node scripts/copy-studio.mjs && astro build",
    "preview": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "types": "wrangler types",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.1",
    "@trokky/client": "^3.4.0",
    "@trokky/studio": "^3.4.0",
    "@trokky/trokky": "^3.4.0",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260914.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.131.2"
  }
}
